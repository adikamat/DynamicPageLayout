Sample Project to draw the QML pages in 2X2 grid. The code allows different sizes of QML pages to fit into multiple rows and / or multiple column.

Sample code to handle focus for each QML page (called as "Widget" here). Load different combinations of widgets dynamically.
